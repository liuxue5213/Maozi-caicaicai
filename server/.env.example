# 服务器端口
PORT=60205

# SQLite 数据文件（建议使用绝对路径）
DATABASE_PATH=/opt/maozi-rps/server/data/maozi-rps.db

# JWT 密钥（生产环境必须修改！）
JWT_SECRET=maozi-rps-secret-key-change-in-production

# CORS 允许的来源（逗号分隔）
CORS_ORIGINS=http://localhost:60200,http://localhost:19006

# 环境
NODE_ENV=production
